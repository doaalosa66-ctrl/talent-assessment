# 系统架构说明

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│                   (http://localhost:3000)                │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│                    前端应用层                             │
│              React + Ant Design + Vite                   │
│  ┌─────────────┬──────────────┬────────────────┐        │
│  │ InputForm   │ ResultsDisplay│  RadarChart   │        │
│  │ (输入表单)   │  (结果展示)    │  (雷达图)      │        │
│  └─────────────┴──────────────┴────────────────┘        │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP/REST API
                       ▼
┌─────────────────────────────────────────────────────────┐
│                   后端服务层                              │
│                 Node.js + Express                        │
│                (http://localhost:3001)                   │
│  ┌───────────────────────────────────────────────┐      │
│  │         assessmentController.js               │      │
│  │          (评估流程控制器)                      │      │
│  └────────────────┬──────────────────────────────┘      │
│                   │                                      │
│  ┌────────────────┴──────────────────────────────┐      │
│  │                                                │      │
│  ▼                                                ▼      │
│  ┌─────────────────────┐      ┌──────────────────────┐ │
│  │ resumeParserService │      │  deepseekService.js  │ │
│  │   (简历解析服务)     │      │    (AI分析服务)      │ │
│  │  - PDF解析          │      │  - 岗位JD分析        │ │
│  │  - Word解析         │      │  - 简历智能解析      │ │
│  │  - 文本解析         │      │  - 候选人评估        │ │
│  └─────────────────────┘      │  - 邀约决策          │ │
│                                │  - 测评题生成        │ │
│                                └──────────┬───────────┘ │
└───────────────────────────────────────────┼─────────────┘
                                            │
                                            ▼
                              ┌─────────────────────────┐
                              │    DeepSeek API         │
                              │ (https://api.deepseek   │
                              │        .com/v1)         │
                              └─────────────────────────┘
```

---

## 数据流转流程

### 阶段1: 用户输入
```
用户 → InputForm组件
  ├─ 输入框1: 公司背景 + 岗位JD
  └─ 输入框2: 上传简历文件(PDF/Word/TXT)
       ↓
    FormData打包
       ↓
  POST /api/assess
```

### 阶段2: 后端处理
```
Express接收请求
  ├─ multer处理文件上传 → 保存到 uploads/
  └─ assessmentController.performAssessment()
       │
       ├─ Step 1: analyzeJobDescription()
       │    ├─ 调用DeepSeek API
       │    └─ 返回: 岗位能力分析
       │
       ├─ Step 2: parseResumeFile() (for each resume)
       │    ├─ 解析PDF/Word/TXT
       │    ├─ 调用DeepSeek API结构化
       │    └─ 返回: 候选人结构化数据
       │
       ├─ Step 3: assessCandidate() (for each candidate)
       │    ├─ 调用DeepSeek API
       │    └─ 返回: 多维评估结果
       │
       ├─ Step 4: generateInvitationDecision() (for each)
       │    ├─ 调用DeepSeek API
       │    └─ 返回: 邀约决策
       │
       └─ Step 5: generateCustomAssessment() (推荐候选人)
            ├─ 调用DeepSeek API
            └─ 返回: 定制化测评题
```

### 阶段3: 结果展示
```
后端返回JSON
  ↓
前端接收
  ↓
ResultsDisplay组件渲染
  ├─ Tab 1: 岗位能力分析报告
  ├─ Tab 2: 候选人综合评估看板
  │    ├─ 评分对比表
  │    └─ 排序和筛选
  └─ Tab 3: 候选人详细分析
       ├─ 基本信息
       ├─ 能力雷达图 (RadarChart组件)
       ├─ 优势和短板
       ├─ 邀约决策
       └─ 定制化测评题
```

---

## 核心模块详解

### 1. DeepSeek服务模块
**文件**: `backend/src/services/deepseekService.js`

**功能**:
- `analyzeJobDescription()` - 岗位JD深度分析
- `parseResume()` - 简历智能解析
- `assessCandidate()` - 候选人多维评估
- `generateInvitationDecision()` - 邀约决策生成
- `generateCustomAssessment()` - 测评题生成

**技术要点**:
- OpenAI SDK集成DeepSeek API
- 精心设计的提示词工程
- JSON安全解析容错机制
- 温度参数优化（0.3-0.7）

### 2. 简历解析模块
**文件**: `backend/src/services/resumeParserService.js`

**功能**:
- PDF解析 (pdf-parse)
- Word解析 (mammoth)
- 纯文本解析 (fs)
- 自动识别文件类型

### 3. 评估控制器
**文件**: `backend/src/controllers/assessmentController.js`

**流程编排**:
1. 验证输入数据
2. 分析岗位JD
3. 批量解析简历
4. 并发评估候选人
5. 生成决策和测评
6. 构建完整报告

### 4. 前端输入组件
**文件**: `frontend/src/components/InputForm.jsx`

**特性**:
- Ant Design表单验证
- 拖拽上传支持
- 文件格式限制
- 实时表单状态

### 5. 前端展示组件
**文件**: `frontend/src/components/ResultsDisplay.jsx`

**展示内容**:
- 分Tab展示5大维度报告
- 数据可视化（雷达图、进度条）
- 响应式布局
- 可折叠详情面板

---

## 技术栈详解

### 后端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | >= 18.0 | 运行时环境 |
| Express | ^4.18 | Web框架 |
| OpenAI SDK | ^4.28 | DeepSeek API集成 |
| multer | ^2.0 | 文件上传 |
| pdf-parse | ^1.1 | PDF解析 |
| mammoth | ^1.6 | Word解析 |
| cors | ^2.8 | 跨域支持 |

### 前端技术栈
| 技术 | 版本 | 用途 |
|------|------|------|
| React | ^18.2 | UI框架 |
| Vite | ^5.0 | 构建工具 |
| Ant Design | ^5.12 | UI组件库 |
| Recharts | ^2.10 | 数据可视化 |
| Axios | ^1.6 | HTTP客户端 |

---

## API接口设计

### POST /api/assess
**功能**: 执行完整的候选人评估流程

**请求**:
```
Content-Type: multipart/form-data

字段:
- companyBackground: string (公司背景)
- jobDescription: string (岗位JD)
- resumes: File[] (简历文件数组)
```

**响应**:
```json
{
  "success": true,
  "data": {
    "job_analysis": {
      "core_competencies": [...],
      "skill_weights": {...},
      "critical_requirements": [...]
    },
    "candidates_overview": [...],
    "candidates_detail": [
      {
        "candidate_info": {...},
        "assessment": {...},
        "decision": {...},
        "custom_assessment": {...}
      }
    ]
  }
}
```

### GET /api/health
**功能**: 健康检查

**响应**:
```json
{
  "status": "ok",
  "message": "路之音智能人才评估系统运行中"
}
```

---

## 安全性设计

### 文件上传安全
- ✅ 文件类型白名单验证
- ✅ 文件大小限制（10MB）
- ✅ 文件数量限制（20个）
- ✅ 随机文件名防止覆盖

### API安全
- ✅ CORS跨域配置
- ✅ 请求体大小限制（50MB）
- ✅ 错误信息脱敏
- ✅ API密钥配置化

### 数据处理安全
- ✅ JSON解析容错
- ✅ 异常捕获和处理
- ✅ 输入验证
- ✅ 日志记录

---

## 性能优化

### 后端优化
- ✅ Promise.all并发处理候选人
- ✅ 流式文件读取
- ✅ 错误快速失败

### 前端优化
- ✅ 组件懒加载
- ✅ 虚拟化长列表（Ant Design Table）
- ✅ 图表按需渲染
- ✅ Vite快速构建

### AI调用优化
- ✅ 温度参数调优
- ✅ max_tokens限制
- ✅ 提示词精简
- ✅ JSON格式强制要求

---

## 扩展性设计

### 易于扩展的部分
1. **新增评估维度**: 修改`assessCandidate`提示词
2. **新增文件格式**: 在`resumeParserService`添加解析器
3. **自定义评分权重**: 修改评估算法
4. **更换AI模型**: 修改`deepseekService`配置

### 预留的扩展接口
- 评估历史记录存储
- 多语言支持
- 报告导出功能
- 邮件通知服务

---

## 部署架构（生产环境建议）

```
              ┌─────────────┐
              │   Nginx     │ (反向代理 + 静态资源)
              └──────┬──────┘
                     │
        ┌────────────┴────────────┐
        │                         │
   ┌────▼────┐              ┌────▼────┐
   │ Frontend │              │ Backend │
   │  (静态)   │              │ Node.js │
   └─────────┘              └────┬────┘
                                  │
                            ┌─────▼──────┐
                            │ DeepSeek   │
                            │    API     │
                            └────────────┘
```

### 推荐配置
- **服务器**: 2核4G起步
- **Node.js**: v18 LTS或更高
- **反向代理**: Nginx
- **进程管理**: PM2
- **日志**: Winston + 日志轮转

---

**架构设计完成，系统可稳定运行！** 🎯
