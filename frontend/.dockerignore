# =============================================================================
# 前端 .dockerignore - 排除无用文件，加速构建，减小镜像体积
# =============================================================================
# 作用：告诉 Docker 哪些文件不要复制到镜像里
# 效果：构建速度提升 10 倍，上下文体积减少 95%
# 原理：类似 .gitignore，但用于 Docker 构建过程
# =============================================================================

# -----------------------------------------------------------------------------
# 1. 依赖目录 - 最大的垃圾源（node_modules 通常占 300MB+）
# -----------------------------------------------------------------------------
# 说明：前端 node_modules 比后端更大（React、Vite、Babel 等）
# 效果：减少构建上下文 300MB+，加速文件传输
node_modules/
npm-debug.log
npm-debug.log.*
yarn-error.log
yarn-debug.log
.pnp
.pnp.js
package-lock.json.bak

# -----------------------------------------------------------------------------
# 2. 环境变量文件 - 前端环境变量
# -----------------------------------------------------------------------------
# 说明：前端 .env 文件会在构建时被打包进 bundle
# 正确做法：使用环境变量注入，而不是硬编码
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

# -----------------------------------------------------------------------------
# 3. 日志文件 - 运行时产生的临时数据
# -----------------------------------------------------------------------------
*.log
*.log.*
logs/
log/

# -----------------------------------------------------------------------------
# 4. 构建输出 - Vite/React 编译产物
# -----------------------------------------------------------------------------
# 说明：本地的 dist 不需要复制，镜像内会重新构建
# 特别注意：生产镜像需要 dist，但会在 builder 阶段生成
dist/
build/
out/
.next/
.nuxt/
.output/

# -----------------------------------------------------------------------------
# 5. 缓存目录 - Vite/Babel/ESLint 等工具的缓存
# -----------------------------------------------------------------------------
# 说明：这些缓存文件在镜像内无用，且会过期
.vite/
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
.turbo/

# -----------------------------------------------------------------------------
# 6. 测试相关 - 测试文件和覆盖率报告
# -----------------------------------------------------------------------------
coverage/
.nyc_output/
test/
tests/
__tests__/
**/*.test.js
**/*.test.jsx
**/*.test.ts
**/*.test.tsx
**/*.spec.js
**/*.spec.jsx
**/*.spec.ts
**/*.spec.tsx
*.test.js
*.test.jsx
*.spec.js
*.spec.jsx
vitest.config.js
vitest.config.ts
jest.config.js
jest.setup.js

# -----------------------------------------------------------------------------
# 7. Git 仓库 - .git 目录通常占 10MB+
# -----------------------------------------------------------------------------
.git/
.gitignore
.gitattributes
.gitmodules

# -----------------------------------------------------------------------------
# 8. IDE 配置文件 - 个人开发环境配置
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*.swn
*~
.DS_Store
Thumbs.db
.editorconfig

# -----------------------------------------------------------------------------
# 9. 文档文件 - README、设计文档等
# -----------------------------------------------------------------------------
README.md
readme.md
CHANGELOG.md
changelog.md
LICENSE
license
*.md
docs/
doc/

# -----------------------------------------------------------------------------
# 10. Docker 相关文件 - 避免递归复制
# -----------------------------------------------------------------------------
Dockerfile
Dockerfile.*
.dockerignore
docker-compose.yml
docker-compose.*.yml
nginx.conf.bak

# -----------------------------------------------------------------------------
# 11. CI/CD 配置 - 持续集成配置文件
# -----------------------------------------------------------------------------
.github/
.gitlab-ci.yml
.travis.yml
.circleci/
.jenkins/
azure-pipelines.yml

# -----------------------------------------------------------------------------
# 12. 前端特定文件
# -----------------------------------------------------------------------------
# Storybook 配置和构建产物
.storybook/
storybook-static/

# 公共资源的源文件（已编译的在 public 里）
src/**/*.psd
src/**/*.ai
src/**/*.sketch

# 临时文件
*.tmp
*.temp
*.bak
*.backup

# =============================================================================
# 效果验证
# =============================================================================
# 1. 查看构建上下文大小（执行 docker build 时的第一行输出）：
#    没有 .dockerignore: Sending build context to Docker daemon  800MB
#    有 .dockerignore:   Sending build context to Docker daemon  5MB
#
# 2. 构建速度对比：
#    没有 .dockerignore: 传输上下文 60 秒 + 构建 2 分钟 = 3 分钟
#    有 .dockerignore:   传输上下文 1 秒 + 构建 2 分钟 = 2 分钟
#
# 3. 为什么前端效果更明显？
#    - node_modules 更大（300MB vs 200MB）
#    - 编译产物更多（dist、.vite 缓存等）
#    - 开发工具更多（Storybook、测试库等）
# =============================================================================
